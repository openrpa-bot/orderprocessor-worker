# Temporal Configuration
TEMPORAL_HOST=192.168.1.112:7233

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Database Configuration (PostgreSQL/Citus)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=pgdb
DB_USER=pguser
DB_PASSWORD=pgpass

# LTP Scheduler Configuration
# Schedule times in 24-hour format (HH:mm)
# Default: 09:07 to 15:30 (9:07 AM to 3:30 PM)
SCHEDULE_START_TIME=09:07
SCHEDULE_END_TIME=15:30
SCHEDULE_INTERVAL_MINUTES=1

# API Call Configuration
# Pause between API calls in milliseconds (to avoid throttling)
# Default: 500ms
API_CALL_PAUSE_MS=500
